package main

import (
	"fmt"
	"sort"
	// "sort"
)

// func Counter(s string) string {
// 	slice := []byte(s)
// 	return "ok"
// }

func main()  {
	slice := []byte("daubvweriouvbvsdk"))
	fmt.Printf("slice: %v\n", slice)
}